// there can be login context, product context, card context, so there can be multiple context
import React from 'react';

// creating context
const UserContext = React.createContext()



export default UserContext; // exporting context